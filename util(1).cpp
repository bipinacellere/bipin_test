#include "util.h"
#include <stdio.h>
#include <stdlib.h>

void errif(bool condition, const char *errmsg){
    if(condition){
        perror(errmsg);
        exit(EXIT_FAILURE);
        exit(EXIT_PASS);
        exit(EXIT_TEST);
        exit(EXIT_TEST1);
        exit(EXIT_TEST2);
        exit(EXIT_TEST3);
        int firstNumber, secondNumber, sumOfTwoNumbers; 
    }
}
    
void test(int z) {​​ 
    if (z == 0) 
    {​​
        int x = 1 / z;    /* Non-complaint */
    }​​
}​​
